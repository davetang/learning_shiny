# Learning about Shiny

[Shiny](https://shiny.posit.co/) is an R package that makes it easy to build interactive web applications straight from R.

## RStudio

Start RStudio Server using Docker.

```console
./bin/run_rstudio.sh
```
